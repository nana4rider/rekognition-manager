# AGENTS.md

## プロジェクト概要

このリポジトリでは、Amazon Rekognitionのマスタメンテナンス用Web UIを開発する。
Rekognitionのコレクション、ユーザー、顔を管理する運用担当者の利用を想定する。

## コミュニケーション

- 大規模な変更や元に戻せない変更を行う前に、変更案を分かりやすい日本語で説明する。
- リポジトリの所有者はバイブコーディング初心者であることを前提とし、馴染みのない用語やコマンドは簡潔に説明する。
- 要件が曖昧な場合は、小さく元に戻しやすい実装を優先し、採用した前提を明示する。
- AWSリソースやセキュリティに影響する仕様を、説明なく決定しない。

## 実装されている機能

- コレクション
  - コレクションを作成する。
  - コレクションを一覧表示する。
  - UI上で明示的な確認を行ってからコレクションを削除する。
- ユーザー
  - コレクション内にユーザーを作成する。
  - ユーザーの詳細を表示する。
  - UI上で明示的な確認を行ってからユーザーを削除する。
  - ユーザー詳細画面で顔の関連付けと関連付け解除を行う。
- 顔
  - 顔を登録する。
  - 顔を一覧表示する。
  - UI上で明示的な確認を行ってから顔を削除する。
- 検索
  - 顔画像からユーザーを検索する
- OIDC(オプション)

## 採用技術

- TypeScriptをstrictモードで使用する。
- フロントエンドにはNext.jsを使用する。
- UIコンポーネントにはMUIを使用する。
- Backend for Frontend(BFF)にはHonoを使用する。
- API契約と実行時の入力検証にはZodを使用する。
- RekognitionへのアクセスにはAWS SDK for JavaScript v3を使用する。
- 静的チェックとフォーマットにはESLintとPrettierを使用する。
- ユニットテストと結合テストにはVitestを使用する。
- BFFのアプリケーションログにはPinoを使用する。
- パッケージマネージャーとモノレポ管理にはnpm workspacesを使用する。
- ローカル環境とデプロイ環境の再現性を確保するためにDockerを使用する。
- 実行時の設定には環境変数を使用する。

## 初期アーキテクチャ

- フロントエンドとBFFを別アプリケーションとして含むモノレポ構成にする。
- 開発時とコンテナ実行時のどちらでも、Next.jsとHonoを別プロセスとして起動する。
- 具体的なデプロイ上の制約が生じない限り、Next.jsとHonoを1つのNode.jsプロセスへ組み込まない。
- ブラウザ側のコードからAWS認証情報とAWS SDKを分離する。Rekognitionの操作はすべてBFFを経由する。
- オニオンアーキテクチャは主にBFFへ適用する。
  - ドメイン層とアプリケーション層からHono、Zod、AWS SDKへ依存しない。
  - Rekognitionの操作は、アプリケーション層との境界にポートまたはインターフェースとして定義する。
  - AWS SDKへのアクセスはインフラストラクチャ層のアダプターとして実装する。
  - Honoのルートは薄く保ち、Zodによる入力検証、ユースケースの呼び出し、結果からHTTPレスポンスへの変換だけを担う。
- フロントエンドとBFFの間では、安定したAPI契約とバリデーションスキーマだけを共有する。インフラストラクチャの詳細は共有しない。

初期のディレクトリ構成は、次の方針に従う。

```text
apps/
  web/       # Next.js + MUI
  bff/       # Hono + Zod + オニオンアーキテクチャ
packages/
  contracts/ # ZodによるAPIスキーマと共有型
```

依存関係のロックファイルには`package-lock.json`を使用し、他のパッケージマネージャーのロックファイルを追加しない。

## AWS設定と機密情報

- ローカル設定では、`AWS_REGION`、`AWS_ACCESS_KEY_ID`、`AWS_SECRET_ACCESS_KEY`を使用できるようにする。
- AWS SDKのデフォルト認証情報プロバイダーチェーンを使用し、アプリケーションの各層へ認証情報を手動で渡さない。
- AWS認証情報をブラウザへ公開しない。機密情報に`NEXT_PUBLIC_`を付けない。
- 機密情報を含む`.env`ファイルは絶対にコミットしない。安全なプレースホルダーだけを記載した`.env.example`をコミットする。
- 利用できる場合は、一時的な認証情報、AWSプロファイル、IAMロールなど、有効期限の短い認証情報を優先する。
- 認証情報、顔画像のデータ、Authorizationヘッダーなどの機密情報をログへ出力しない。
- 実装したRekognition操作に必要な最小限のIAM権限だけを要求する。

## APIとUIの動作

- コレクションIDとユーザーIDを信頼できない入力として扱い、BFFとの境界で検証する。
- リクエストとレスポンスのデータはスキーマで検証する。TypeScriptの型だけに依存しない。
- BFFは一貫した形式のJSONエラーを返し、AWS内部の詳細やスタックトレースを漏らさない。
- AWSリソースの削除や関連付け解除を行う操作では、UI上に確認手順を設ける。
- 読み込み中、データなし、成功、エラーの各状態をUI上で分かるようにする。
- AWSの一覧取得処理ではページネーションへ対応し、1回のAPIレスポンスですべて取得できると仮定しない。
- 必要なAWS固有の識別子は維持しつつ、ユーザーには理解しやすいラベルとエラーメッセージを表示する。

## ログとローカルデバッグ

- BFFではPinoによる構造化ログを使用する。
- ローカル環境では`pino-pretty`で読みやすく整形し、本番環境ではJSON形式のログを標準出力へ出力する。
- HonoのRequest IDミドルウェアでリクエストIDを生成し、リクエスト単位のPino子ロガーへ含める。
- リクエストログ、後続処理のログ、AWS SDK呼び出しのログに同じリクエストIDまたは相関IDを含める。
- AWS SDK呼び出しでは、操作名、成否、処理時間を記録する。リクエストやレスポンスの全体をそのまま出力しない。
- Authorizationヘッダー、Cookie、OIDCトークン、AWS認証情報、顔画像、Base64データをPinoのredaction対象とし、ログへ出力しない。
- ドメイン層とアプリケーション層はPinoへ直接依存せず、小さなLoggerインターフェースを介してログを出力する。
- WebとBFFの両方について、デバッグ用コマンドまたはエディターの起動設定を用意し、ドキュメントへ記載する。
- 本番コードに一時的な`console.log`を残さない。

## テストと品質

- アプリケーション層のユースケースとドメインルールにユニットテストを追加する。
- AWS SDKクライアントをモックしてインフラストラクチャ層のアダプターをテストする。テストから実際のAWSリソースを変更しないことをデフォルトとする。
- 入力検証、HTTPステータスコード、エラー変換についてAPIレベルのテストを追加する。
- 重要な運用フローと削除確認についてフロントエンドのテストを追加する。
- 変更を完了とする前に、関連するフォーマッター、リンター、型チェック、テストを実行する。
- 変更は小さく、目的を絞る。無関係な書き換えを行わない。
- 全角英数、半角カナ、一部の全角記号`（）／＄＃＆`は原則利用しない。

## ドキュメント

- 前提条件、セットアップ、環境変数、ローカル開発、デバッグ、テスト、Dockerの使用方法をREADMEに記載し、実装に合わせて更新する。
- 特に認証やデプロイに関する重要な設計判断は、`docs/adr/`配下へ短いADR(Architecture Decision Record)として記録する。
- 実装とこのファイルの記載が一致しなくなった場合は、同じ変更の中でドキュメントも更新する。
